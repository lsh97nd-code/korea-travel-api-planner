제출용 README.md 빈칸 채우기 목차

힌트: 실제 프로그램이 데이터를 어떤 순서로 주고받는지 위 흐름도를 자신의 파일명과 API에 맞게 수정하세요.

2-1. API 간 데이터 연결 방법

[여기에 작성]

힌트: 첫 번째 LLM 결과의 recommended_city 값을 꺼내서 "도시명 + 맛집" 형태로 장소 검색 API에 전달한다고 설명하세요.

3. 프로젝트 폴더 구조

힌트: 실제 제출 ZIP에 들어 있는 파일과 폴더를 그대로 적으세요. README에만 있고 ZIP에는 없는 파일이 생기지 않도록 마지막에 반드시 비교하세요.

3-1. 주요 파일 설명
파일	역할
[ ]	[ ]
[ ]	[ ]
requirements.txt	[ ]
.env.example	[ ]
.gitignore	[ ]
results/	[ ]

힌트: 각 Python 파일이 CLI, LLM 호출, 장소 검색, 리포트 생성 중 무엇을 담당하는지 한 줄씩 적으세요.

4. 설치 방법
4-1. Python 버전 확인

실제 확인 버전:

힌트: Python 3.10 이상이어야 합니다.

4-2. 가상환경 생성 및 활성화 ⭐
Windows PowerShell
macOS / Linux

힌트: 필수 요구사항은 아니지만 다른 제출물처럼 가상환경 사용법까지 적어두면 재현하기 쉽습니다.

4-3. 필요한 패키지 설치

requirements.txt:

힌트: 실제 코드에서 import하는 외부 패키지를 적으세요. 빈 requirements.txt를 제출하지 않도록 확인하세요.

5. API 제공자
5-1. LLM API

사용 API:

사용 모델:

선택 이유:

[여기에 작성]

힌트: 왜 이 LLM API를 선택했는지 1~2문장으로 적으세요.

5-2. 지도/장소 검색 API

사용 API:

선택 이유:

[여기에 작성]

힌트: 국내 맛집 검색이 가능하고 JSON 응답을 받을 수 있다는 점을 설명하세요.

5-3. 장소 데이터 필드
프로그램 필드	API 원본 필드	설명
name	[ ]	장소명
address	[ ]	주소
category	[ ]	카테고리
url	[ ]	장소 URL
x 또는 lng	[ ]	경도
y 또는 lat	[ ]	위도

힌트: 실제 장소 API 응답에서 어떤 값을 가져와 프로그램의 맛집 정보로 저장하는지 적으세요.

6. REST API 요청/응답 구조
6-1. REST API란?

[여기에 작성]

힌트: 클라이언트가 HTTP 요청을 보내고 서버가 상태 코드와 JSON 같은 응답을 돌려주는 방식이라고 자신의 말로 설명하세요.

6-2. GET과 POST의 차이
구분	GET	POST
주요 목적	[ ]	[ ]
이 프로그램에서 사용한 곳	[ ]	[ ]
전달 방법	[ ]	[ ]

힌트: 장소 검색처럼 데이터를 조회할 때 GET을, LLM에 프롬프트를 보내 새로운 결과를 생성할 때 POST를 사용한다고 설명하면 이해하기 쉽습니다.

6-3. 이 프로그램의 API 호출
LLM 1차 추천
HTTP 방식: [ GET / POST ]
입력: [ ]
출력: [ ]
장소/맛집 검색
HTTP 방식: [ GET / POST ]
검색어: [ ]
출력: [ ]
최종 LLM 리포트
HTTP 방식: [ GET / POST ]
입력: [ ]
출력: [ ]

힌트: 실제 사용한 SDK가 내부적으로 어떤 HTTP 방식으로 요청하는지도 설명할 수 있으면 좋습니다.

7. API 키 설정
7-1. 필요한 환경변수

힌트: 실제 키는 절대로 적지 말고 환경변수 이름과 YOUR_KEY 같은 예시 값만 적으세요.

7-2. .env 사용

.env 예시:

힌트: .env.example에는 변수 이름만 보여주고 실제 API 키는 넣지 마세요.

7-3. Windows PowerShell 환경변수 설정

힌트: 현재 PowerShell 세션에서 API 키를 설정하는 방법을 적으세요.

7-4. macOS / Linux 환경변수 설정

힌트: 과제 문서에서 요구하는 환경변수 설정 예시를 실제 사용하는 API 변수명에 맞게 바꾸세요.

8. API 키 보안
8-1. API 키를 코드에 작성하지 않는 이유
[ ]
[ ]
[ ]

힌트: 협업 중 키 공개 방지, 키 교체 시 코드 수정 불필요, 과금·쿼터 사고 예방의 세 가지 이유를 자신의 말로 적으세요.

8-2. .gitignore

힌트: .env가 Git에 올라가지 않게 하고, 가상환경이나 __pycache__도 필요하면 제외하세요.

8-3. 제출 전 보안 확인 ⭐
 Python 코드에 실제 API 키 없음
 README에 실제 API 키 없음
 실행 로그에 실제 API 키 없음
 JSON에 실제 API 키 없음
 Markdown 결과에 실제 API 키 없음
 .env 제출 안 함
 .env.example에는 가짜 값만 있음
 .gitignore 파일명이 실제로 .gitignore인지 확인

힌트: 다른 제출물에서 gitignore.txt나 env.example처럼 파일명이 잘못된 사례가 있었으므로 실제 파일명까지 확인하세요.

9. CLI 사용 방법
9-1. 기본 실행

실제 실행 예:

힌트: 과제에서 -date "YYYY-MM-DD"가 필수이므로 실제 프로그램에서도 이 방식이 동작하는지 확인하세요.

9-2. 정상 실행 화면

힌트: 실제 실행했을 때 터미널에 표시되는 진행 로그와 결과 저장 경로를 적으세요.

9-3. 날짜 입력 검증

정상 입력:

잘못된 입력:

잘못된 날짜 입력 시 결과:

힌트: 형식이 잘못되면 사용법을 보여주고 프로그램을 종료한다는 것을 실제 실행 결과로 적으세요.

9-4. 필수 옵션 누락 ⭐

결과:

힌트: 날짜 자체를 입력하지 않았을 때 argparse가 필수 옵션 누락을 안내하는지도 확인하면 좋은 증빙이 됩니다.

9-5. 도움말 ⭐

힌트: 사용 가능한 CLI 옵션을 확인할 수 있는 화면을 적거나 캡처해두세요.

10. 1차 LLM 여행지 추천
10-1. 입력

힌트: 실제 테스트에 사용한 여행 날짜를 적으세요.

10-2. 프롬프트 설계

[여기에 작성]

힌트: 날짜를 전달하고 설명문이나 Markdown이 아니라 JSON으로 파싱 가능한 결과만 반환하도록 어떤 지시를 했는지 설명하세요.

10-3. 필수 JSON 스키마

힌트: recommended_city와 weather, 문자열 배열 events 1~3개, 추천 근거 2~4문장이 들어가도록 작성하세요.

10-4. 실제 1차 추천 결과

힌트: 실제 LLM이 반환한 결과를 보여주되 키나 민감정보가 포함되지 않았는지 확인하세요.

10-5. 다음 API로 연결

힌트: LLM JSON이 단순 저장용이 아니라 다음 API의 입력으로 사용된다는 것을 보여주세요.

11. 맛집 검색
11-1. 검색 입력

힌트: 실제 추천 도시를 이용해 만든 검색어를 적으세요.

11-2. 검색 개수

힌트: 과제 권장값은 5곳입니다.

11-3. 맛집 데이터 구조

또는:

힌트: 사용 API에 맞게 x/y 또는 lat/lng를 사용하고 가능한 필드를 최대한 보존하세요.

11-4. 검색 결과 0건 처리

리포트:

힌트: 0건은 프로그램 오류로 종료하지 말고 맛집을 데이터 없음으로 만든 뒤 최종 리포트까지 계속 생성해야 합니다.

12. 최종 여행 리포트 생성
12-1. LLM 입력 데이터
1차 여행 추천 JSON
맛집 검색 결과

힌트: 두 데이터를 합쳐 두 번째 LLM 요청에 전달한다고 적으세요.

12-2. 최종 Markdown 구성

힌트: 추천 지역·추천 이유·날씨·행사·맛집·1일 일정이 모두 들어가야 합니다. 맛집이 0건이면 반드시 데이터 없음으로 표시하세요.

13. 오류 처리
13-1. 오류 처리 원칙

[여기에 작성]

힌트: try-except를 사용하고, 반드시 멈춰야 하는 오류와 기록만 하고 계속 진행할 오류를 구분했다고 설명하세요.

13-2. API 키 미설정

처리 방법:

[ ]

힌트: 키가 없으면 즉시 종료하고 사용자가 .env 또는 환경변수 설정 방법을 알 수 있게 안내해야 합니다.

13-3. LLM JSON 파싱 실패

첫 번째 실패:

재시도:

힌트: JSON 파싱이 실패하면 프롬프트를 수정하여 최대 1회만 다시 요청해야 합니다. 무한 재시도는 하면 안 됩니다.

13-4. 장소 API 인증 오류
HTTP 401

[여기에 작성]

HTTP 403

[여기에 작성]

힌트: 키 값, 키 종류, 권한 또는 필요한 서비스 설정을 확인하도록 안내하세요.

13-5. API 쿼터 오류
HTTP 429

[여기에 작성]

힌트: 호출 한도 초과임을 기록하고 맛집을 데이터 없음으로 처리한 뒤 가능한 경우 리포트는 계속 생성하세요.

13-6. 네트워크 오류

[여기에 작성]

힌트: Timeout이나 연결 실패를 try-except로 잡아 어떤 방식으로 errors에 기록하는지 적으세요.

13-7. 서버 오류 ⭐
HTTP 5xx

[여기에 작성]

힌트: 500/502/503 같은 외부 서버 오류를 어떻게 처리하거나 제한적으로 재시도하는지 적으세요.

13-8. 검색 결과 0건

힌트: 0건도 errors에 기록할 수 있지만 프로그램은 종료하지 않는다는 것을 보여주세요.

13-9. 오류 목록 관리

힌트: 오류가 없어도 errors: []가 존재하도록 만들면 결과 구조가 일정해집니다.

14. 결과 저장
14-1. 결과 폴더

힌트: 프로그램 실행 시 results/가 없으면 자동으로 생성하도록 구현했는지 적으세요.

14-2. 원본 JSON

힌트: 최소한 1차 추천 JSON, 맛집 검색 리스트, errors 배열이 모두 들어가야 합니다.

14-3. 최종 Markdown

파일:

힌트: 실제 생성된 .md 파일명을 적으세요.

15. 보너스 1 — 복수 지역 추천
15-1. 구현 내용

[여기에 작성]

힌트: recommended_city 하나를 2~3개 지역으로 확장하고 각 지역을 반복문으로 처리했다고 설명하세요.

15-2. JSON 구조

힌트: 각 지역의 추천 정보와 맛집 정보를 서로 구분할 수 있게 구조를 설계하세요.

15-3. 지역별 맛집 검색

힌트: for 반복문으로 지역마다 장소 API를 호출한 결과를 적으세요.

15-4. 지역별 최종 리포트

[여기에 작성]

힌트: 여러 도시의 맛집과 일정이 섞이지 않도록 지역별 소제목으로 구분했다고 설명하세요.

16. 보너스 2 — 결과 캐싱
16-1. 구현 목적

[여기에 작성]

힌트: 같은 날짜로 반복 실행할 때 불필요한 API 비용과 실행 시간을 줄이기 위해 구현했다고 적으세요.

16-2. 캐시 처리 흐름

힌트: 같은 날짜의 원본 JSON이 있으면 API를 다시 부르지 않는 흐름을 적으세요.

16-3. 실제 캐시 실행 결과

힌트: 같은 날짜를 두 번 실행하여 두 번째 실행에서 캐시를 사용했다는 로그를 적으세요.

16-4. 캐시 무시 기능 ⭐

힌트: --no-cache 또는 --refresh 같은 옵션을 추가했다면 사용 방법을 적으세요.

17. 테스트
17-1. 테스트 항목
테스트	예상 결과	실제 결과
정상 날짜	정상 실행	[ ]
날짜 형식 오류	사용법 출력 후 종료	[ ]
필수 -date 누락	오류 후 종료	[ ]
API 키 미설정	설정 안내 후 종료	[ ]
LLM JSON 파싱 실패	1회 재시도	[ ]
맛집 검색 성공	N곳 반환	[ ]
맛집 0건	데이터 없음 + 계속 실행	[ ]
장소 API 401/403	오류 기록 + 계속 실행	[ ]
네트워크 오류	오류 기록	[ ]
캐시 재실행	API 호출 생략	[ ]
복수 지역	지역별 맛집 검색	[ ]

힌트: 성공 사례뿐 아니라 실패 사례도 실제로 테스트했다는 것을 보여주세요.

18. 실행 결과 예시
18-1. 정상 실행

힌트: [1/3] → [2/3] → [3/3] → 완료 → 저장 경로가 보이도록 적으세요.

18-2. 장소 검색 실패

힌트: 오류가 발생해도 최종 리포트까지 생성되는 것이 보이게 작성하세요.

19. 과제를 통해 학습한 내용
19-1. REST API

[여기에 작성]

힌트: 요청·응답 구조와 GET/POST 차이를 직접 구현하면서 무엇을 이해했는지 적으세요.

19-2. JSON을 이용한 API 연결

[여기에 작성]

힌트: LLM의 JSON 결과를 다음 장소 API의 입력으로 사용하면서 배운 점을 적으세요.

19-3. API 오류 처리

[여기에 작성]

힌트: 인증·쿼터·네트워크·파싱 오류는 원인과 처리 방법이 서로 다르다는 점을 적으세요.

19-4. API 키 보안

[여기에 작성]

힌트: .env/환경변수를 사용하면 키 공개를 막고 코드 수정 없이 키를 교체할 수 있으며 과금 사고도 예방할 수 있다는 점을 자신의 말로 적으세요.

19-5. 복수 API를 연결하면서 배운 점

[여기에 작성]

힌트: 한 API의 출력이 다른 API의 입력이 되는 데이터 파이프라인을 직접 만든 경험을 정리하세요.

20. 문제 해결 과정 ⭐
20-1. 발생한 문제

[여기에 작성]

20-2. 원인

[여기에 작성]

20-3. 해결 방법

[여기에 작성]

20-4. 해결 결과

[여기에 작성]

힌트: 실제 개발 중 경험한 401/403/429/503, JSON 파싱 오류, 모델 오류 같은 사례 중 실제로 발생한 것만 적으세요.

21. 추가 구현 사항 ⭐
21-1. 도시명 정규화

[구현했다면 작성]

힌트: "강원특별자치도 강릉시"처럼 긴 지역명을 장소 검색에 적합한 형태로 정리했다면 설명하세요.

21-2. API 오류 메시지에서 키 마스킹

[구현했다면 작성]

힌트: 오류 메시지에 API 키가 포함될 경우 [REDACTED] 등으로 바꾸는 기능이 있으면 설명하세요.

21-3. API 서버 오류 제한 재시도

[구현했다면 작성]

힌트: 429/503 등에 무한 재시도가 아닌 제한된 횟수의 재시도를 구현했다면 적으세요.

21-4. 장소 검색 API 모듈화

[구현했다면 작성]

힌트: Kakao 관련 코드를 별도 함수나 파일로 분리하여 나중에 Naver 같은 API로 교체하기 쉽게 만들었다면 설명하세요.

21-5. LLM 모듈화

[구현했다면 작성]

힌트: 추천 생성과 최종 리포트 생성 기능을 별도 함수/모듈로 나눴다면 설명하세요.

21-6. 결과 메타데이터 ⭐

힌트: 생성 시각, 캐시 사용 여부, 맛집 개수 같은 정보를 추가하면 결과를 검증하기 편합니다.

22. 증빙자료
번호	파일명	확인 내용
01	[ ].png	Python 버전
02	[ ].png	가상환경
03	[ ].png	패키지 설치
04	[ ].png	프로젝트 구조
05	[ ].png	CLI 구현
06	[ ].png	정상 실행
07	[ ].png	날짜 오류
08	[ ].png	API 키 보안
09	[ ].png	LLM JSON
10	[ ].png	맛집 검색
11	[ ].png	0건 처리
12	[ ].png	API 인증 오류
13	[ ].png	원본 JSON
14	[ ].png	최종 Markdown
15	[ ].png	복수 지역
16	[ ].png	캐싱

힌트: image1.png처럼 짓지 말고 07_날짜형식오류.png, 10_맛집검색성공.png, 14_최종리포트.png처럼 파일명만 봐도 내용을 알 수 있게 만드세요.

23. 제출 파일 최종 확인
 CLI Python 프로그램이 들어 있음
 -date "YYYY-MM-DD"가 동작함
 날짜 오류 시 사용법 출력 후 종료됨
 LLM API가 실제 연결됨
 LLM 결과가 JSON으로 파싱됨
 recommended_city가 다음 API에 사용됨
 장소 API가 실제 연결됨
 맛집 데이터가 저장됨
 맛집 0건에서도 프로그램이 계속됨
 최종 Markdown 리포트가 생성됨
 오전/오후/저녁 1일 일정이 있음
 try-except 오류 처리가 있음
 API 키 미설정 시 즉시 종료함
 JSON 파싱 실패 재시도는 최대 1회임
 장소 API 실패 시에도 리포트를 생성함
 errors 배열이 있음
 results/가 생성됨
 원본 JSON 1개 이상을 실제 제출함
 최종 Markdown 1개 이상을 실제 제출함
 .env를 제출하지 않음
 실제 API 키가 코드에 없음
 실제 API 키가 README에 없음
 실제 API 키가 로그에 없음
 실제 API 키가 JSON/Markdown에 없음
 .env.example에는 실제 키가 없음
 .gitignore가 실제 파일명으로 존재함
 requirements.txt가 비어 있지 않음
 README 내용과 실제 ZIP 구조가 일치함
 보너스 1: 2~3개 지역 추천 구현
 보너스 1: 각 지역별 맛집 검색 구현
 보너스 1: 리포트를 지역별로 정리
 보너스 2: 동일 날짜 JSON 캐싱 구현
 보너스 2: 캐시 사용 시 API 호출을 건너뜀
